{
  "name": "reserve-va-offset-calculator",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
  "build": "npm run build -w ./packages/calc-engine && npm run build -w ./apps/web-pwa",
  "dev": "npm run dev -w web-pwa"
}
