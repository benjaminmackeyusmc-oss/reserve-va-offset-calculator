{
  "name": "reserve-va-offset-calculator",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @rvo/calc-engine && npm run build -w web-pwa",
    "dev": "npm run dev -w web-pwa"
  }
}
